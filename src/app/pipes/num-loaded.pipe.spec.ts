import { NumLoadedPipe } from './num-loaded.pipe';

describe('NumLoadedPipe', () => {
  it('create an instance', () => {
    const pipe = new NumLoadedPipe();
    expect(pipe).toBeTruthy();
  });
});
